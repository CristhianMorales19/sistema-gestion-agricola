trigger:
- main

pool:
  name: Default
  demands:
    - Agent.Name -equals DESKTOP-NUK01OF

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '16.x'
  displayName: 'Instalar Node.js'

- script: |
    dir
    cd backend
    dir
    npm install
    npm run test
  displayName: 'Listar archivos y ejecutar pruebas'